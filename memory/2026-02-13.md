# 2026-02-13

- Configuramos integração do Telegram no OpenClaw.
- Inicialmente não funcionou porque o canal estava configurado, mas o plugin `plugins.entries.telegram.enabled` estava `false`.
- Corrigido habilitando o plugin e reiniciando o gateway via `config.patch`.
- Verificação final com `openclaw status --deep`: Telegram ON/OK, bot detectado como `@Crislpdsbot`.
